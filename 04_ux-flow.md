# UXフロー仕様: ガイド付き点検フロー

## 1. 全体フロー
```
① 型式・年式を入力
   ↓
② チェック順リストを生成
   - 汎用チェックリスト（全車共通・MVPで実装）
   - ＋ 型式別の既知の弱点情報（LLM APIで都度生成、拡張機能）
   ↓
③ 1項目ずつ案内 → 写真/動画/音声をアップロード
   ↓
④ 項目ごとに判定方式を振り分け
   - 自動AI判定（④色差、⑥水没）→ 即時結果表示
   - 未対応項目（③膜厚、⑦エンジン音等）→ 「記録として保存」のみ、または将来の有償人的レビュー
   ↓
⑤ 全項目終了後、総合レポート生成（PDF、サブスク機能と連携）
```

## 2. 設計思想（重要）
「収集フロー」と「判定方法」を分離する。
- 収集フロー（型式入力〜順番に案内〜アップロード）は全項目分をローンチ初日から用意できる
- 判定方法は項目ごとに精度が異なるため、対応済み/未対応をUI上で明示し、段階的に精度を上げていく
- これにより「AI判定が完成していない項目があるからローンチできない」という事態を避けられる

## 3. 型式別チェックリストの生成方法
- 型式ごとの弱点データベースを自前で構築するのはコストが大きい
- 代替案: **LLM APIに型式・年式を渡し、「既知の弱点・注意点」をその場で生成させる**
  - 例: 「〇〇年式△△（型式□□）の中古車で注意すべき点は？」というプロンプトをAPI経由で投げ、結果を汎用チェックリストに上乗せする
  - 精度は参考情報レベルだが、データベース構築なしでパーソナライズが可能

## 4. エンジン音・動画の扱い（MVP方針）
- 動画アップロードなら音声トラックも同時取得できるため、収集自体は容易
- ただしAIによるエンジン異音の自動判定は環境ノイズの影響が大きく、精度確保が難しい
- MVPでは「録音・録画してレポートに添付」までを役割とし、**自動判定は行わない**（将来的に人的レビュー等の有償オプションとして拡張）

## 5. 未対応項目の表示方針
判定精度が確立していない項目については、ユーザーに誤った安心感を与えないよう、UI上で明確に区別する。
- 例: 「AI診断結果」バッジ（④⑥など） vs 「記録のみ・参考情報」バッジ（③⑦など）
